/**
 * @params startIndex: index inicial da string que o valor do campo se encontra
 * @params endIndex: index final da string que o valor do campo se encontra
 * @params name: nome do campo no objeto que será criado
 */

export interface FieldLineDTO {
    startIndex: number
    endIndex: number
    name: string
}